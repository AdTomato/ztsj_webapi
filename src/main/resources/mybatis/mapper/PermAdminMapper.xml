<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper>
    <select id="getPermAdminByUserId" parameterType="java.lang.String">
        select id from h_perm_admin where userId = #{value}
    </select>
</mapper>